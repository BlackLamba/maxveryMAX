распределение модулей:
  1. чат-бот - Антон
  2. БЭК - Женя
  3. ФРОНТ - ДИМА
  4. ВОРКЕР - олежа.

Примерный ТУДУ лист на первое время:
этот ужас был накидан за 5 минут, ребята, не стесняйтесь менять, просто на всякий случай помечайте в этом файле и антону что вы изменили, особенно если в поле ответственности другого.

> Обновление: по итогам обсуждения с командой сбор параметров (город/дата/интересы/бюджет) переехал из бота в мини-приложение — см. 06_MAX_и_UX.md. Список ниже пересобран под это решение: у бота стало сильно меньше диалоговой логики, вместо неё — уведомления и часть общей инфраструктуры (закрывает то, что раньше висело без владельца).

Общее:
Создать репозиторий и структуру папок: bot/, backend/, worker/, miniapp/, shared/, infra/ - пример;
docker-compose.yml skeleton: postgres + backend + worker + bot + miniapp - пример;
.env.example без токенов;
shared/models.py — Pydantic-модель Event, City, Category - пример;
shared/categories.py и shared/categories.ts — единый enum категорий;
Схема БД: миграции для cities, categories, events, event_sources, tags, event_tags, providers, users, favorites, user_preferences, event_changes (для уведомлений) - пример;
Зафиксировать OpenAPI-контракт (можно markdown-таблицей на первом этапе);
Формат deep link бот → мини-апп: теперь без параметров фильтров (они собираются в самом мини-аппе) — уточнить в актуальной документации MAX, как мини-апп получает контекст пользователя (id/сессия) при открытии;
Общее правило: воркер пишет в БД, бэк читает из БД, бот и мини-апп ходят только в бэк;

Воркер:
worker/data/mock_events.json — 30–50 событий с полями из модели Event;
worker/providers/base.py — интерфейс EventProvider;
worker/providers/mock.py — читает JSON;
worker/pipeline/normalize.py — raw → Event;
worker/pipeline/validate.py — проверка обязательных полей;
worker/repository.py — upsert в events, event_sources, tags, event_tags, providers;
CLI-команда: python -m worker import --file data/mock_events.json;
Проверка: после запуска в БД появились события;
worker/pipeline/deduplicate.py — RapidFuzz по title + дата + адрес;
quality_score — простая формула, записывается в events;
worker/scheduler.py — периодический запуск (interval);
Обработка ошибок провайдера (retry, логирование);
seed.py — наполнение БД для бэка одной командой;
Юнит-тесты на нормализацию и дедупликацию;
(после базового импорта) При повторном импорте фиксировать изменения цены/времени/места уже существующих событий в таблицу event_changes — это единственный источник данных для уведомлений, без него бэку нечего будет отдавать боту;

Приоритет для Олежи: сначала рабочий import JSON → БД (разблокирует Женю и Диму), потом — дедупликация, quality_score, scheduler, и только в последнюю очередь — event_changes для уведомлений (Could Have, не блокирует основной сценарий);

Бэк:
FastAPI-скелет + подключение к PostgreSQL;
GET /api/health;
GET /api/cities;
GET /api/categories;
GET /api/events с фильтрами: city_id, date_from, date_to, price_max, category;
GET /api/events/{id};
POST /api/search — упрощённый, принимает те же фильтры;
Простое ранжирование: совпадение категории + времени + цены + свежесть;
Поле reason в ответе — «почему подходит»;
Уточнение фильтров: временное окно, радиус (если есть координаты);
Сортировки: по расстоянию, по цене, по релевантности;
POST /api/favorites + GET /api/favorites + DELETE /api/favorites/{event_id};
POST /api/preferences + GET /api/preferences;
POST /api/events/{id}/click — лог перехода пользователя на внешний источник. Без этого нечем измерить метрику «доля пользователей, дошедших до перехода» (см. 08_пилотный_запуск.md);
Обработка пустого результата: возвращать [] + подсказку смягчить фильтры;
Корректные HTTP-коды и формат ошибок;
(Should/Could, не Must — не тратить на них первый день) POST /api/search/natural — простой парсер фразы в фильтры;
(после базового пути) GET /api/notifications/pending и POST /api/notifications/{id}/ack — сопоставление event_changes с favorites, отдаёт боту, что нужно разослать (см. раздел 4 в 06_MAX_и_UX.md);

Ботик (статус на 19.09 — обязательная часть готова):
✅ Регистрация бота в MAX — токен берётся из переменной окружения MAX_BOT_TOKEN (заглушка в .env.example, реальное значение — локально в .env, не в репозитории);
✅ /start и событие bot_started (кнопка «Начать» в профиле бота) — приветствие + объяснение назначения + inline-кнопка «🎟 Открыть подборку» типа open_app, открывающая привязанное к боту мини-приложение. Пейлоад с параметрами фильтров НЕ передаётся (по новой архитектуре их собирает сам miniapp), state machine убран;
✅ /help с краткой инструкцией;
✅ /reset — убран, т.к. у тонкого бота больше нет состояния диалога, которое нужно сбрасывать;
✅ Тексты вынесены в bot/texts.py, конфиг — в bot/config.py (аналогично worker/config.py), асинхронный HTTP-клиент к backend — в bot/api_client.py (бот не ходит в БД и к внешним афишам);
✅ Точка входа bot/main.py: long polling, регистрация хендлеров, фоновый цикл уведомлений;
✅ bot/requirements.txt и bot/Dockerfile в стиле остальных модулей (python:3.12-slim, контекст сборки — корень app/);
✅ .env.example дополнен переменными бота (MAX_BOT_TOKEN, BOT_NAME, MINIAPP_BASE_URL, NOTIFICATIONS_POLL_INTERVAL_SECONDS); в docker-compose.yml сервис bot подхватывает эти переменные;
✅ README.md (раздел 5.2 и связанные пункты) переписан под актуальную тонкую архитектуру (вход + уведомления), старая версия про 4 шага и state machine удалена;

⏳ Модуль уведомлений (Could Have, не блокирует демо) — код написан (bot/notifications.py): периодический опрос GET /api/notifications/pending, рассылка, POST .../ack, обработка блокировки бота пользователем (логируется, цикл не падает). Пока:
  • эндпоинты GET /api/notifications/pending и POST /api/notifications/{id}/ack на бэке не реализованы — цикл не падает, просто пропускает итерации;
  • в уведомлениях пока нет inline-кнопки открытия карточки — добавится, как только бэк начнёт отдавать поле с deep link/startapp payload для конкретной карточки события;
⏳ Docker-инфраструктура и README по чек-листу трека — основная часть (Dockerfile bot, compose, .env.example, раздел 5.2 README) готова; общий README трека (сценарий проверки, порты) после прогона end-to-end.

Отклонения от docs/02_user_journey.md / 06_MAX_и_UX.md — см. заметку в конце коммита (параметр в deep link не нужен, т.к. мини-апп получает identity пользователя автоматически через initData из MAX Bridge; URL мини-приложения прописывается в настройках на платформе MAX для партнёров, а не передаётся в кнопке из кода).

Фронт:
Vite + React + TypeScript + MAX UI;
HashRouter (для WebView);
API-клиент к бэкенду;
Экран приветствия/фильтров: город или геолокация (с обработкой отказа в разрешении — предлагать выбрать город вручную), календарь (диапазон или конкретные дни), интересы кнопками-чипсами с множественным выбором, поле бюджета (можно оставить пустым или указать «бесплатно»);
Экран ленты активностей: изображение, название, цена, место, расстояние (если есть геолокация), кнопка «в избранное» прямо из ленты;
Компонент EventCard;
Экран карточки события: полная информация, «почему подходит», кнопка перехода к источнику с формулировкой, явно обозначающей переход вовне (не «Купить», а «Перейти к покупке» — см. 06_MAX_и_UX.md про границу Won't Have);
Кнопка «Перейти к источнику» → внешняя ссылка, с логированием клика в бэк перед переходом;
Возврат к экрану фильтров и изменение параметров без обращения к боту;
Сортировки;
Избранное: добавление/удаление, экран «Избранное»;
Обязательные состояния: Loader, EmptyState, ErrorState (включая пустой результат — подсказка смягчить фильтры);
Скелетоны карточек;
Баннер о результате действия;
Адаптив: 360 / 375 / 390 / 414 / 430 px;
Проверка на реальном iPhone и Android;
Проверка в веб-версии MAX;
Интеграция MAX Bridge (определение платформы, запрос геолокации);
Локальный запуск через Vite dev server;

Общее (без конкретного владельца — решить на созвоне):
Итоговая сверка: кто именно проверяет сборку docker-compose end-to-end перед сдачей.
