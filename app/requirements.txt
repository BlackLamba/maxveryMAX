# --- Web-фреймворк / HTTP API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# --- База данных (SQLAlchemy 2.x, async) ---
sqlalchemy[asyncio]>=2.0,<3.0
alembic>=1.13                # миграции схемы БД
asyncpg>=0.29                # PostgreSQL (основной режим: docker compose)
aiosqlite>=0.20              # SQLite — локальный запуск без Docker (make dev / dev-fill)

# --- Валидация и конфигурация ---
pydantic>=2.7
pydantic-settings>=2.2       # чтение .env / переменных окружения (config.py)

# --- Dev и тесты (cd backend && python -m pytest) ---
httpx>=0.27                  # асинхронный HTTP-клиент для тестов API
pytest>=8.2
pytest-asyncio>=0.23